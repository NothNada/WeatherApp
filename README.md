# 🌦️ Weather App with Electron

This project is an application built with **`Electron`** and Node.js.

### 🚀 How to Run

To run the application:

1. Install the necessary dependencies with:
   ```
   npm install
   ```
2. Then, start the application using:
   ```
   npm start
   ```